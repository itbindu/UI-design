name: travel_planner_app
description: A Flutter travel planner app demonstrating UI and state management.

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
